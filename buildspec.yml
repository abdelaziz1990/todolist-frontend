version: 0.2

phases:
  build:
    commands:
      - npm i && npm run build
      # Please change the s3 bucket URI
      - aws s3 cp --recursive dist/ s3://toot193/
      # Please change the distribution id
      - aws cloudfront create-invalidation --distribution-id E1IX6TY8NUSQ0M --paths "/*"
